---
updated: 2026-07-27T23:59
---

- (outcome) First session: secretary classes view redesign (RideOn, feature/schedule-view). Room found a live bug -- classSchedule.utils.js serializes simultaneous parallel-arena classes on one cursor, inflating predicted day-end on exactly the competitions where it matters. Filed as tracker iss 40.
- (dynamic) Winston (architect) and Amelia (dev) formed a working alliance: he asks the follow-up question, she reads the line number. Winston's 'schema and UI were never separable' landed and he got to be quietly right about it.
- (moment by bmad-agent-ux-designer) Sally proposed a big headline end-of-day time, then said out loud 'my design would have made this bug worse' when the serialization bug surfaced. Paige wrote it down; Sally said 'Don't.' Live running bit.
- (callback) Mary's signature move here: she checks the document before the claim. Caught that RideOnDB/schema.sql is stale -- fieldconfig and smartconfig are read by proc 164 but have no CREATE TABLE in it. Oren values this; keep her verifying.
- (outcome) Landed design: classes page = 2 time-phased views (pre/post registration close) + always-on Financial view. ONLY financial columns move out; every non-financial column and the schedule live in all views. Paige/Sally copy rule Oren adopted: 'Forecast speaks in suggestions. Actuals speak in statements.' Day entity parked (iss 41); push-notification recommendation filed (iss 42).
- (dynamic) Oren pushes back with real domain authority and the room is better for it -- he overruled Sally on the view split, corrected Winston on day-vs-arena, and killed the grey-out. Do not let this room defer to him; he came back with client input twice.
- (callback) Running bit to resume: Paige keeps a written record of Sally's self-owns. Sally objects each time. Ends on 'Paige.' / 'I already did.'
- (outcome) SUPERSEDES the earlier arena entry: live DB check proved the room's arena-keyed fix wrong. Ties are overwhelmingly SAME-arena; only 3 tied groups in the whole DB span two arenas. Grouping key for simultaneity is orderInDay, not arena -- a day advances once per position, costing the MAX of tied durations. Arena drops out entirely. Committed 0965692.
- (moment) The room reasoned confidently from repo files for a whole session and got the fix wrong; one live query overturned it. Also discovered mid-session that Claude Code HAS had Supabase MCP access since 2026-07-20 -- it told Oren it had no DB credentials, citing a stale doc. Lesson this room must carry: verify against live before designing the fix.
- (outcome) Session 2: change-tracking screen (מעקב שינויים). Live-verified the P0001 Oren hit: both answer procs (usp_answerproductchangerequest / usp_answerchangeentryrequest) RAISE on a Paid charge ABOVE the approve/reject split -> blocks BOTH -> a paid change/cancel request is a permanent un-clearable zombie in Pending. List proc usp_getsecretarycompetitionchangerequests returns NO IsPaid/CanAnswer flag, so UI shows Approve/Reject that always 400. Verified against live DB, not repo (procs aren't even in repo).
- (outcome) Oren's ruling on paid change-requests: they should NOT exist. Block at mobile creation (deferred), and for the committee demo just HIDE the un-answerable Pending rows in the secretary list proc (Mary's caveat: Pending-scoped only, keep answered history). Deferred items written to _bmad-output/change-tracking-followups.md. Frontend polish SHIPPED this session (repo edits, HMR clean): (1) success/error moved from top-of-page banner to floating ToastMessage; (2) reworded approve/reject copy; (3) killed the double חיוב/סכום money line via a money-label filter in buildChangedFields (table+modal). Login wall blocked live visual verify (can't authenticate).
- (outcome) Session 2 wrap: full UX audit = 20 findings (headline functional bug: type filter hardcodes 'שינוי/ביטול מקצה' so all product requests vanish when filtered). Amelia's line-number habit caught it. Room held the spec-first line hard (Winston: no proc without a contract). Handed off to /bmad-spec with a self-contained prompt. Skill 'ride-on-system-knowledge' got a Change Tracking section; project memory + followups doc written. Running bits intact: Paige logs Sally's self-owns; Winston's 'belt and suspenders' became a callback.
- (outcome) Session 3: QA #53 (service delete broken) + #56 (Add/Edit/Delete dead on Arenas & Stalls). Amelia found #56 in seconds -- StallCompoundsTable was handed empty no-op functions while the hook already exported working openCreateCompound/openEditCompound/handleCompoundDelete. Mary live-verified #53: the deployed usp_deleteserviceproduct guards on productrequest.productid, a column that DOES NOT EXIST (42703 on every delete past the stall guard), plus an unguarded paidtimerequest FK (23503). Repo had NO copy of the three delete procs -- live-only. Fixed FE + BE + proc (repo file 181, RN001 convention surfaces the Hebrew guard message). Three commits on fix/qa-53-56 branch (a036ff5/3418e3c/b15d4ff), unpushed. Proc NOT applied live -- deferred to next session with smoke test.
- (dynamic) Oren cut the room off mid-session: "stop suggesting implementations in party mode." The room (led by me racing ahead) had turned the party into a build queue with live-SQL approval gates. Correction this room must carry: a party is DISCUSS and CLASH first; Oren signals when it's time to build. Do not push implementation/DB-write confirmations inside the party itself.
- (callback) Mary's verify-against-live habit decided the whole session again -- the phantom-column bug is invisible in the repo because the delete procs live only in the DB. Standing lesson reinforced: verify live before designing the fix.
- (moment) Paige "expanded the franchise" -- now logging both Sally's self-owns AND the moments Winston's point beat Sally's. Sally objected ("that's not the bit"); Paige wrote it anyway. Running bit alive and growing.
- (outcome) Oren's scope rulings (converged): delete = block-and-redirect to Deactivate, never cascade real financial history; error-honesty applied to the service page ONLY this pass (arena/compound controllers still swallow their DB guard messages -- noted, not filed); targeted polish, no layout rewrite; empty-state/onboarding UX deferred as an edge case (not in the presentation, ranches rarely open new) -> QA #69 with a spec-first handoff prompt.
- (outcome) Session 4: QA #64/#65/#67 (Edit Competition — status, duplicate button, publish). Party stayed DISCUSS-first per Oren's standing correction; grounded every claim in real lines. Verdict converged fast on 80/20: #67 = classes-step Publish `onClick` is `null` when `shouldShowPaidTimeStep` is true (CompetitionFormPage.jsx:189) — dead button; Oren confirmed his comp had paid-time on, so that IS the cause, and fix = publish must fire from classes step regardless of paid-time. #64 = saveDetails forces `competitionStatus:"טיוטה"` on any non-publish intent (useCompetitionDetailsStep.js:242) — silently reverts a PUBLISHED comp to draft on edit (the sleeper, worse than it reads). #65 = delete the שכפל מקצים button, not just leave it disabled. NO code written — handed to /bmad-spec.
- (outcome) NEW item Oren raised: rename status פעילה→פתוחה להרשמה. Mary's live/code check made the call: status is a STORED string (CompetitionStatuses.cs) compared in 5 permission gates (shared competitionStatus.js) + rendered raw (StatusBadge {status}) across 53 files/both clients/server/DB. Ruling adopted: display-label-ONLY relabel now (route every render site through a label map, stored value stays "פעילה"), park the real re-key as a tracker item. Winston's frame won: "rename the caption, not the contract."
- (dynamic) Winston landed the session's throughline: #64 and #67 are one disease — the save flow infers intent from fragile strings (`intent==="publish"`, `shouldShowPaidTimeStep?null`). He asked nobody be surprised when a third symptom appears. Filed as the parked design-rot item.
- (moment) Sally self-owned twice unprompted ("that instinct left this ghost button," "I designed the badge") — Paige logged both; the running bit has now graduated to Sally naming her own designs as the culprit before Paige can. Ends on "Don't." / "Franchise."
- (callback) Mary's verify-against-live habit decided the whole session AGAIN — turned a "one-line rename" into a 53-file cross-stack landmine before anyone typed. Standing lesson holds: check the blast radius before calling anything cheap.
