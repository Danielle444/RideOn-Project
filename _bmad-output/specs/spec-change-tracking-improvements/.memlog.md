---
topic: Secretary-web Change Tracking page (מעקב שינויים): pre-committee polish + two functional fixes (hide paid zombies, fix type filter)
updated: 2026-07-27T18:52
---

- (direction) Input is rich (user-supplied 15 in-scope items + verified system facts). Express distill, no elicitation. Interactive presentation.
- (constraint) Scope: pre-committee polish (~2 weeks out) + exactly two functional fixes. Tighten hard; defer everything in change-tracking-followups.md.
- (constraint) The 4 change-tracking procs are LIVE-ONLY (not in repo). Verify body live via Supabase MCP before any edit; every DB write shown as exact SQL, confirmed, re-read after.
- (constraint) Authoritative 'paid' definition (verified live 2026-07-27): billcharge.chargestatus='Paid' with sourcetype='Entry'/sourceid=originalentryid (entry) or sourcetype='ProductRequest'/sourceid=originalprequestid (product). Hide-filter MUST mirror this exactly.
- (constraint) Hide-filter must be Pending-scoped in the list proc (answered history stays visible); count proc is already pending-only so just add the not-exists.
- (constraint) Answer-proc P0001 paid-guards stay in place as backstop; hiding rows is defense-in-depth, not a replacement.
- (constraint) Any SQL pasted to Supabase must be committed to repo. After any .cs change run dotnet build. Hebrew RTL UI throughout.
- (decision) Already-shipped uncommitted frontend polish (ToastMessage, reworded approve/reject copy, de-duped money line) is context only; do NOT redo.
- (capability) CAP-1: Paid un-answerable Pending requests never surface. Add not-exists(paid billcharge) to BOTH list-proc branches (Pending-scoped) AND mirror in count proc so badge matches.
- (capability) CAP-2: Fix type filter. ChangeRequestsFilters hardcodes entry-only RequestType strings, hiding ALL product requests; filter on IsCancelled boolean instead.
- (capability) CAP-3: Details modal translates Status to Hebrew (aushra/nidhta) matching the table; no raw English Status anywhere.
- (capability) CAP-4: Map backend/Postgres errors (P0001 etc.) to Hebrew user messages; never surface raw proc text in UI.
- (capability) CAP-5: Hebrew copy corrections: rename jargon labels (seh-hakol batab, entity/yeshut, lahar ishur), harmonize 4 entry/product x change/cancel phrasings, trim over-promising filter subtitle. Exact strings in hebrew-strings.md companion.
- (capability) CAP-6: Empty state distinguishes 'none exist' vs 'none match filter' with a clear nudge to clear filters.
- (capability) CAP-7: Confirmation prompt before Approve (moves money / can trigger a fine); consider one before Reject.
- (capability) CAP-8: Row action spinner shows loading label only on the pressed button, not both.
- (capability) CAP-9: Date ranges render in correct order in RTL (earlier date first); fix bidi.
- (capability) CAP-10: Post-start cancellations that keep the full charge show a one-line explanation label.
- (capability) CAP-11: Summary consolidates 5 cards / two overlapping breakdowns into one breakdown + a total.
- (decision) Companions: hebrew-strings.md (all copy/status/error strings), backend-changes.md (exact proc SQL deltas + deploy/verify protocol + type-filter fix location). Adopt existing change-tracking-followups.md for deferred scope.
- (note) Non-goals: block-at-mobile-creation, secretary refund/edge handling, dead camelCase dual-casing cleanup, buildChangedFields extraction, pagination, broader a11y (all deferred in change-tracking-followups.md).
- (event) Self-validate Pass 1 (Coherence): PASS. 11 capabilities each have intent+success; success criteria are demonstrable at the demo. Constraints all bend design (live-only procs, exact paid-mirror, Pending-scope, column-shape preservation). 4 non-goals explicit. Success signal is a concrete demo walkthrough. IDs CAP-1..CAP-11 unique/stable. Prose lean.
- (event) Self-validate Pass 2 (Preservation): PASS. All 15 in-scope items landed: functional #1->CAP-1, #2->CAP-2; leaks #3->CAP-3, #4->CAP-4; copy #5/6/8/9->CAP-5+hebrew-strings.md, #7->CAP-5 harmonize table; feedback #10->CAP-6, #11->CAP-7, #12->CAP-8, #13->CAP-9, #14->CAP-10, #15->CAP-11. Verified proc bodies + exact paid-definition captured in backend-changes.md. Deferred items adopted via change-tracking-followups.md. Already-shipped polish flagged do-not-redo. No load-bearing drops.
- (event) Spec finalized: SPEC-change-tracking-improvements, 11 capabilities, 2 spec-authored companions (hebrew-strings.md, backend-changes.md) + 1 adopted (change-tracking-followups.md), 1 assumption-set + 3 open questions.
- (decision) Open Q1 RESOLVED: Oren approved all recommended Hebrew wording in hebrew-strings.md as-is. Remove all (needs sign-off) markers; strings are final/approved.
- (decision) Open Q2 RESOLVED: search box matches RequestedByName + EntityName + BeforeText + AfterText (verified in hook getRequestSearchText). Subtitle finalized as 'חיפוש לפי שם מבקש או פרטי הבקשה' - accurate.
- (decision) Open Q3 RESOLVED: Reject also requires a confirmation dialog (not Approve-only). CAP-7 now mandates confirmation on BOTH Approve and Reject.
- (event) Spec re-derived after Oren resolved all 3 open questions (2026-07-27): CAP-7 now requires confirmation on both Approve and Reject; all Hebrew strings approved final; subtitle finalized. No open questions remain. Spec ready for bmad-quick-dev implementation.
