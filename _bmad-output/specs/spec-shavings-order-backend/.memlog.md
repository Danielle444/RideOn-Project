---
topic: Spec 1 — Shavings order-table & backend follow-through: kill secretary approval, repurpose responsetime/arrivaltime as seen/delivered, decouple delivery from photo, Pending→Seen→Delivered vocabulary, worker-mobile pipeline, proc/DAL/data migration, repo hygiene
updated: 2026-07-24T21:15
---

- (direction) Two-spec split (ratified 2026-07-24, owner Oren): this is Spec 1 = shavings order-table + backend/DB + worker-mobile follow-through; Spec 2 = secretary web page redesign (separate); #31/#46 notification push fully out of scope.
- (constraint) Hard: NO column renames and NO column drops on live shavingsorder. responsetime and arrivaltime repurposed by NOTE ONLY (physical names unchanged). approvedbypersonid/approvedat left in place, just stop reading/writing.
- (constraint) DB changes must stay backward-compatible with the CURRENTLY-DEPLOYED backend and installed mobile app. Per CLAUDE.md: verify live proc signatures (pg_get_functiondef) before any CREATE OR REPLACE / DROP; recovered-shavings-procs.live.sql is the verbatim-from-live reference.
- (capability) CAP-1 Retire secretary approval entirely: drop procs usp_getpendingdeliveryapprovals(#5)+usp_approvedelivery(#6); remove DAL GetPendingDeliveryApprovals/ApproveDelivery, BL methods, controller endpoints pending-approvals + approve-delivery, DTOs PendingDeliveryApprovalItem/ApproveDeliveryRequest, web service getPendingDeliveryApprovals/approveDelivery + the page's approve action. Photos are proof-on-demand, not a gate.
- (capability) CAP-2 responsetime = 'seen by worker': usp_claimshavingsorder additionally sets responsetime=now() (and deliverystatus='Seen'). responsetime had ZERO references anywhere so it is safe to start writing.
- (capability) CAP-3 arrivaltime = 'delivered-at', the canonical delivery timestamp that DRIVES status, decoupled from the photo. Never written today; becomes the delivery clock.
- (capability) CAP-4 Photo optional proof + no-photo delivery path: new proc usp_markdelivered stamps arrivaltime=COALESCE(arrivaltime,now())+deliverystatus terminal WITHOUT touching photo. 'unverified' = delivered (arrivaltime set) AND deliveryphotourl IS NULL — DERIVED, no new column.
- (decision) No-photo path v1 scope: usp_markdelivered proc + endpoint SHIP IN V1 so a delivery is always recordable even if photo upload fails. Worker-app surfaces the no-photo button as a FALLBACK after a photo-upload failure (recommended), keeping the photo path primary so it does not cannibalize proof; a co-equal prominent button is deferred. (Open question flags the alternative.)
- (capability) CAP-5 Status vocabulary Pending->Seen->Delivered. Canonical terminal token switched from 'Closed' to 'Delivered' (self-documenting; 'Closed' meant approval-closed, a retired concept). deliverystatus is the display token written in lockstep with responsetime/arrivaltime so they never diverge.
- (decision) Data migration of ~10 live rows: WaitingApproval(2)+Closed(2) -> 'Delivered' with arrivaltime backfilled from deliveryphotodate (both have photodate). Pending(6): claimed-but-undelivered (workersystemuserid NOT NULL, arrivaltime NULL) -> 'Seen'; unclaimed stay 'Pending'.
- (capability) CAP-6 usp_savedeliveryphoto STOPS writing 'WaitingApproval'; instead sets deliverystatus='Delivered' + arrivaltime=COALESCE(arrivaltime,now()) + attaches deliveryphotourl/date. COALESCE means a late photo promotes an unverified delivery to verified WITHOUT moving the delivery clock. Return type VOID unchanged (CREATE OR REPLACE safe).
- (capability) CAP-7 usp_getshavingsordersforcompetitionandranch STOPS returning ApprovedByPersonId/ApprovedAt; ADDS Seen(=responsetime), Delivered(=arrivaltime), PrequestDatetime(SLA source). Return-type change => DROP FUNCTION + CREATE. New columns appended LAST.
- (constraint) SEQUENCING (critical): CAP-7 column removal breaks the currently-deployed DAL, which reads reader["approvedbypersonid"]/["approvedat"] by name (ShavingsOrderDAL.cs:345-353). The read-proc change must be deployed WITH the backend deploy, NOT independently ahead of it.
- (capability) CAP-8 Worker read procs usp_getworkershavingsorders(113) + usp_getshavingsordersforworkerbycompetition(114) gain a ResponseTime output column (append LAST) so the app can render 'Seen'. Return-type change => DROP+CREATE. Adding a trailing column is backward-compatible with the deployed DAL (reads by name).
- (capability) CAP-9 Worker mobile state machine Pending->Seen->Delivered: drop the 'ממתין לאישור מזכירה' (WaitingApproval) limbo state from WorkerShavingsOrderCard + both worker screens; claim ('קח טיפול') = Seen; deliver via photo ('צלם ואשר אספקה') and no-photo fallback; update getStatusLabel + the deliverystatus button branches (currently keyed on literal Pending/WaitingApproval/Closed).
- (constraint) SEQUENCING (critical): CAP-2/CAP-5 make claim write 'Seen' and deliver write 'Delivered'. The DEPLOYED mobile card branches on literal tokens and only shows the deliver button when deliveryStatus==='Pending' (WorkerShavingsOrderCard.jsx:86). Writing the new tokens BEFORE the worker-app release leaves claimed/delivered orders unactionable on installed apps => the token-writing migration must land with the mobile release.
- (capability) CAP-10 Reconcile ShavingsOrderDAL.cs to ONE convention: converge the raw static NpgsqlCommand methods onto CreateCommandWithStoredProcedure positional-dict helper (CLAUDE.md-mandated). Document the jsonb exception for CreateShavingsOrder if the helper's AddParameterWithType lacks jsonb support.
- (capability) CAP-11 Repo hygiene: split recovered-shavings-procs.live.sql into one-file-per-proc under RideOnDB/StoredProcedures/PostgreSQL/Individual/ (NNN_usp_*.sql); DELETE retired 115_usp_GetPendingDeliveryApprovals.sql + 116_usp_ApproveDelivery.sql; renumber 114_usp_SaveDeliveryPhoto off the 114 collision; assign non-colliding numbers (next free 168+) to the 9 recovered procs + new usp_markdelivered; reconcile against the folder at apply time.
- (assumption) Assumed mobile updates reach devices via Expo OTA (EAS Update), making the worker-app release near-instant and the token-sequencing window small. If updates are app-store only, the sequencing risk (CAP-9 constraint) widens materially.
- (question) Legacy responsetime backfill: for claimed-undelivered rows reclassified to 'Seen', the true seen-time is unknown (responsetime was never written). Leave responsetime NULL (status Seen, no seen-SLA clock) or backfill a placeholder? Only affects test rows now.
- (question) No-photo button prominence in v1: fallback-only after photo failure (recommended) vs a co-equal standalone 'mark delivered' button? Decision governs whether the fallback cannibalizes photo proof.
- (question) Mobile release mechanism (Expo OTA vs app-store) governs the safe timing of the token-writing migrations (CAP-2/5/9) — needs confirmation to schedule the deploy.
- (event) Sources absorbed: shavings-data-layer-map.md (§0 status, §5 ratified decisions) + recovered-shavings-procs.live.sql (9 verbatim-from-live procs). Grounded against ShavingsOrderDAL.cs, ShavingsOrder.cs, ShavingsOrdersController.cs, worker mobile screens/card/service, committed procs 113/114/115, web shavings service+page.
- (note) memlog seeded; rendering SPEC.md + companions (change-set, state-machine, repo-hygiene, spec-2-handoff).
- (event) SELF-VALIDATE Pass 1 (Coherence, Spec Law 1-6,8): PASS. 11 capabilities each carry intent+success; constraints all bend design (no-rename, two sequencing gates, verify-live, live-write discipline); 4 non-goals; success signal is a testable field walkthrough; CAP IDs unique. Minor: CAP-2/CAP-6 intents name the specific column write because that write IS the observable 'what' at the data layer — mechanism detail deferred to change-set.md.
- (event) SELF-VALIDATE Pass 2 (Preservation): PASS. All 7 ratified decisions + verified blast-radius facts + worker-mobile scope + architecture constraints + hand-off list landed in SPEC.md or a companion. Two NEW load-bearing findings added beyond the sources: (a) deployed DAL reads approvedby*/approvedat by name -> CAP-7 must ship with backend deploy; (b) deployed mobile card branches on literal tokens -> token migration must ship with app release. Wrapper-only content dropped: the slash-command invocation ceremony.
- (event) Spec finalized (create). SPEC.md + 4 companions (change-set.md, state-machine.md, repo-hygiene.md, spec-2-handoff.md). 11 capabilities, 1 assumption, 3 open questions.
- (decision) OPEN-Q RESOLVED (Oren): legacy responsetime backfill -> leave NULL. Claimed-undelivered legacy rows carry no seen-SLA clock; acceptable (test rows only).
- (decision) OPEN-Q RESOLVED (Oren): no-photo delivery button is FALLBACK-ONLY (surfaces after a photo-upload failure), NOT a co-equal standalone button. Photo stays primary.
- (decision) OPEN-Q RESOLVED (Oren delegated to Claude): token-migration strategy = RELEASE-MECHANISM-AGNOSTIC / backward-compatible-by-design. Stored deliverystatus carries only {Pending, Delivered}; do NOT write a 'Seen' token. 'Seen' is DERIVED (workersystemuserid IS NOT NULL AND arrivaltime IS NULL; responsetime is its clock). Rationale: installed old apps key the deliver button on deliveryStatus==='Pending' and only recognize Pending/WaitingApproval/Closed; keeping the order 'Pending' through claim means old apps keep working through the whole flow even if the app update lags store review. Supersedes the earlier 'deliverystatus in lockstep, writes Seen at claim' design and its hard mobile sequencing gate.
- (decision) Consequence of backward-compat design: usp_claimshavingsorder sets workersystemuserid + responsetime=now() ONLY (deliverystatus untouched, stays 'Pending'). Data migration drops the Pending->Seen UPDATE entirely; only WaitingApproval/Closed -> Delivered (+ arrivaltime backfill from deliveryphotodate) remains. Interim cosmetic: an un-updated old app briefly shows the raw 'Delivered' badge for delivered orders (no green checkmark) — benign, order needs no worker action.
- (decision) Server-side sequencing gate for CAP-7 (read-proc column removal must ship WITH the backend deploy) STILL STANDS — both are in one server release, so it is controlled. Only the MOBILE hard-gate is removed by the backward-compat design.
- (event) SPEC re-derived after open-question resolutions: CAP-2/CAP-5 revised (Seen derived, not a stored token), lockstep + mobile-sequencing constraints revised, data migration simplified, Assumptions/Open-Questions sections cleared (all resolved). Re-validated: Spec Law + preservation still PASS.
- (event) Wrote implementation-handoff.md — ready-to-paste bmad-quick-dev GOAL prompt (backend+frontend, run until CAP-1..CAP-11 done). Spec 1 authoring complete.
