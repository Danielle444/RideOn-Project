---
topic: QA #53/#56 — secretary Arenas & Stalls compound Add/Edit/Delete wiring + Service Prices delete (block-and-redirect proc + honest RN001 error surfacing). Proc APPLIED LIVE 2026-07-30. Remaining work: verify E2E, merge in one deploy window.
updated: 2026-07-30
---

- (direction) Source: QA #53/#56 party-mode handoff (2026-07-27), branch fix/qa-53-56-arenas-stalls-service-delete. Oren: spec covers #53/#56 ONLY; QA #69 onboarding is deliberately EXCLUDED. Scope = honesty bet + restrained bet.
- (note) Grounded vs code: ArenasAndStallsPage.jsx compound handlers now wired to real hook fns (QA #56 shipped). 183_usp_DeleteServiceProduct.sql rewritten (guards usage via pricecatalog for productrequest.pricecatalogid AND paidtimerequest.pricecatalogid; raises Hebrew RN001; hard-deletes only zero-stall/zero-order products). C# committed; proc NOT yet applied live.
- (capability) CAP-1 (#56): Secretary can Add/Edit/Delete stall compounds on the Arenas & Stalls page. Success: 'הוספת מתחם' opens create modal, Edit opens prefilled, Delete confirms then removes (or shows booking-guard message). Implemented.
- (capability) CAP-2 (#53 unused): Secretary can delete a never-used service product; hard-deleted with no crash and no financial-history loss. Success: deleting a product with zero stalls and zero orders removes its product/pricecatalog/paidtimeproduct rows and returns success.
- (capability) CAP-3 (#53 used): Deleting an in-use service product (assigned to stalls, or ordered via productrequest OR paidtimerequest) is blocked with a clear Hebrew 'deactivate instead' message and no data loss. Success: deleting e.g. paid-time product 1 or 2 surfaces the Hebrew guard via BadRequest — no 42703/23503 crash, no generic string, history intact.
- (constraint) Block-and-redirect, never cascade: used products are deactivated not deleted; the proc never destroys financial history (Oren's ruling).
- (constraint) Usage must be detected through pricecatalog for BOTH request types (productrequest.pricecatalogid and paidtimerequest.pricecatalogid); the old productrequest.productid column does not exist (was the 42703 crash).
- (constraint) Honest error surfacing: SQLSTATE RN001 -> DAL rethrows BL.ValidationException -> controller returns BadRequest(message). The Hebrew guard text must reach the user, not a generic string.
- (constraint) Deploy coupling: fully-correct behavior needs BOTH the new proc applied live AND the C# deployed (merge -> Render) in the same deploy window. Neither ordering degrades below today.
- (constraint) Live-DB protocol: apply proc via show-SQL -> explicit go-ahead -> apply_migration -> re-read + side-effect-free smoke test; exact rollback body captured before applying (see companion).
- (note) Non-goals: QA #69 onboarding UX (deliberately excluded); applying the RN001/ValidationException honesty pattern to Arena/Compound delete controllers (scoped to service page only — known accepted follow-up); cascade delete / any financial-history destruction.
- (assumption) The C# changes (ServicePriceDAL RN001 catch, ServicePricesController ValidationException->BadRequest) are committed and compile clean (handoff: dotnet build passes, only file-lock MSB noise). Spec treats them as the backend contract to deploy.
- (decision) Companion migration-and-deploy.md holds the operational runbook (new-proc summary + path, exact rollback body, smoke-test approach, deploy-coupling ordering). SPEC.md kernel stays prose-only.
- (event) Self-validate Pass 1 (coherence): PASS. 3 caps each have intent+success; intents are WHAT; 5 constraints each bend design; 3 explicit non-goals; success signal demonstrable on deployed system; IDs unique. No blocking open questions (remaining work is gated execution).
- (event) Self-validate Pass 2 (preservation): PASS. Absorbed: #56 root cause+fix, #53 root causes A(42703 phantom column)+B(23503 paidtimerequest FK)+generic-message swallow, proc rewrite behavior, DAL/controller RN001 chain, rollback body, smoke test, deploy coupling, E2E criteria. Wrapper-only/deferred (correctly NOT in caps): QA #69 (non-goal), Arena/Compound controller consistency follow-up (non-goal).
- (event) SPEC.md + migration-and-deploy.md companion + handoff-prompt.md derived and finalized: 3 capabilities, 1 companion, 1 assumption, 0 open questions. Superseded a mistaken first attempt (spec-arenas-stalls-onboarding / #69) which was removed at Oren's direction — #69 excluded on purpose.
- (event) 2026-07-30 — PROC APPLIED LIVE with Oren's explicit go-ahead, migration `qa53_fix_deleteserviceproduct_guards_rn001`. CREATE OR REPLACE, oid 21844, signature unchanged; deployed body byte-identical to the repo file (prosrc md5 1b36cbffb32c9ee44cad876f682386da, 1240 chars). Pre-apply smoke test ran in a rolled-back txn (terminating RAISE) covering all four paths — unused product 11 clean-deleted across all 3 child tables, product 2 (160 paidtimerequest rows) RN001 not 23503, product 3 (128 stalls) RN001, id 9999 RN001 not-found — and the rollback was re-read and confirmed (counts restored, throwaway fn absent). Post-apply against the DEPLOYED proc: products 2, 3, 5 all raise RN001 with the Hebrew guard. The 42703 crash is gone. CAP-2/CAP-3 are satisfied at the DB layer.
- (event) 2026-07-30 — Proc file renumbered 181 -> 183: `181_usp_GetRealHorsesByRanch.sql` was already on main and 182 was claimed by `182_usp_ApplyAutoScheduleV2.sql` on a concurrent branch. Same collision class as the earlier 164 clash. All 7 references updated. Cite 183 from now on, never 181.
- (finding) 2026-07-30 — Read-only audit: only FIVE FKs touch product/pricecatalog (stall->product, pricecatalog->product, paidtimeproduct->product, productrequest->pricecatalog, paidtimerequest->pricecatalog). The new proc guards or deletes all five, so there is NO residual FK hole beyond the two named in the constraint above. Verified, not inferred.
- (finding) 2026-07-30 — Branch divergence audit: `fix/qa-53-56-db-and-verify` (cut from the original fix branch) is 6 commits behind main and 6 ahead, with ZERO file overlap, so a conflict-free merge is expected. Main's only change inside the #53 path is `ServicePriceManager.cs`, and it is purely additive (new `GetActiveServicePricesForRanch` for the mobile consumer view); `DeleteProduct` is untouched. A rebase is therefore not required for CAP-1/2/3 correctness. Not merged and not rebased — Oren withheld both.
- (note) 2026-07-30 — E2E (CAP-1/2/3 in a browser) was NOT run; Danielle declined the UI checks and deferred them to Oren post-deploy. CAP-1 is verified by static tracing only: the three handlers exist and are returned by `useArenasAndStallsPage`, `StallCompoundsTable` invokes `onCreate`/`onEdit`/`onDelete`, and the modal prefills via `initialItem={page.editingCompound}` + `isEdit`. Deployed app remains in the runbook's "new proc + old C#" row until the merge: correct blocking, generic Hebrew message.
- (note) 2026-07-30 — Local-dev blocker for this machine: `rideon-local.ps1` does NOT exist anywhere on Danielle's box (`C:\Users\betka`; whole-drive search) and none of the six backend env vars are set at session/User/Machine scope. `appsettings.json` has no ConnectionStrings and no Jwt section, and `appsettings.Development.json` is TRACKED in git (contrary to CLAUDE.md, which calls it gitignored) with only logging + App:WebBaseUrl. A local backend there needs the connection string and Jwt__Key pulled from Render first. The script is Oren's, on Oren's machine.
- (note) 2026-07-30 — SPEC.md still says the #53 proc is "not yet applied to the live database" in its Why narrative. Left UNCHANGED deliberately: the SPEC is the canonical contract and the guardrail says fix the implementation, not the spec. Flagged to Oren as her call.
