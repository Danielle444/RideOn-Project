---
topic: Spec 2 - Shavings page redesign (secretary web): all-orders view replacing the retired approval queue, ranch/status grouping toggle (#29), SLA delay flagging (#30), secretary add-order form with mandatory ranch (#32). Frontend/UX only; consumes Spec 1 (spec-shavings-order-backend) vocabulary and reads.
updated: 2026-07-24T20:15
---

- (event) Sources absorbed: shavings-data-layer-map.md (sec 5 ratified decisions) + Spec 1 SPEC.md/spec-2-handoff.md/state-machine.md (dependency contract) + recovered-shavings-procs.live.sql. Grounded against web CompetitionShavingsPage.jsx+shavingsOrderService.js, competition-summary components/hook/service, mobile admin add-order (ShavingsOrderModal/useAdminCompetitionShavings/CompetitionShavingsTab), ShavingsOrdersController.cs. Live DB reads for booking-ranch distribution.
- (direction) Two-spec split: this is Spec 2 = secretary web page redesign ONLY (frontend/UX). Spec 1 (spec-shavings-order-backend) owns the data layer, procs, worker-mobile, approval removal. Notification push #31/#46 out of scope.
- (note) KEY FINDING: the current CompetitionShavingsPage.jsx IS the secretary approval-queue screen (calls getPendingDeliveryApprovals/approveDelivery). Spec 1 deletes that action+service. So the redesign REPLACES the approval queue with an all-orders view; there is no order list on that page today.
- (constraint) Adopt Spec 1 vocabulary verbatim: deliverystatus in {Pending, Seen, Delivered} (terminal Delivered, no Closed/WaitingApproval). Unverified delivery = arrivaltime NOT NULL AND deliveryphotourl NULL (derived, no column). Approval is gone: never render ApprovedByPersonId/ApprovedAt, approve button, or approval queue.
- (constraint) Author NO new stored procedures. Reuse Spec 1 reads. If a new read/field is unavoidable, flag it as a Spec 1 DEPENDENCY (DEP-N), never inline SQL. Web reaches API via shared axiosInstance -> ASP.NET controllers -> BL -> DAL -> procs.
- (decision) LIVE-GROUNDED read model: (R1) getCompetitionSummaryShavingsDetails(competitionId, hostRanchId) [proc #172] enumerates booking ranches + rollup for group headers. (R2, per booking ranch) getShavingsOrdersForCompetitionAndRanch(competitionId, bookingRanchId) [proc #176, post-CAP-7] returns per-order rows WITH the SLA fields (DeliveryStatus, Seen, Delivered, PrequestDatetime) + pricing; concatenate across ranches into the master list, tag each row with BookingRanch. (R3 optional, on row expand) summary entries #173 / order details #174 for HorseNames/PayerNames/IsPaid/per-stall. hostRanchId = activeRole.ranchId (same as summary page).
- (event) Live DB check: ALL shavings orders (competitions 7/8/44) and ALL 23 stallbookings have booking_ranchid == hostranchid (only Double K/11). So group-by-ranch is degenerate (1 group) on current data and a single R1+R2 pair returns everything; multi-ranch must still be built generically because the summary procs model N booking ranches as a first-class dimension.
- (decision) Grouping toggle (#29): group=ranch (natural, header from R1 rollup) or group=status (client-side regroup of the master list into Pending/Seen/Delivered buckets). Toggle + optional ranch/status filter live in URL state via React Router v7 useSearchParams (default group=ranch). Note: useSearchParams is only thinly used in web (3 auth/superuser pages) - this establishes the pattern for secretary pages.
- (decision) SLA #30: named constant SHAVINGS_SLA_THRESHOLD_HOURS = 3. Rule A (unclaimed too long): deliverystatus='Pending' AND now()-PrequestDatetime > 3h. Rule B (seen but undelivered too long): Seen set, Delivered NULL, now()-Seen > 3h. A flagged order gets BOTH in-row highlight AND a dedicated 'needs attention' (דורש טיפול) section pinned at top so delays are spotted at a glance. Clock is SEEN, not approved (approval removed).
- (question) OQ #30 wording: original issue said 'approved within 3h'; approval is removed so the clock is SEEN. Prompt explicitly asks to confirm wording with Oren. Proceeding with seen-based clock; label pending confirmation.
- (capability) CAP-1 All-orders page: scrap the approval-queue page; new page lists EVERY shaving order for the competition (all booking ranches), replacing the deleted approval UI.
- (capability) CAP-2 Ranch<->Status grouping toggle in URL state (#29): group master list by booking Ranch or by Order Status (Pending->Seen->Delivered); default ranch; toggle+filters persisted in URL search params.
- (capability) CAP-3 Lifecycle rendering (#29, consumes Spec 1): status chips Pending/Seen/Delivered; created/seen/delivered timestamps; unverified-delivery flag (delivered, no photo); zero approval affordances anywhere.
- (capability) CAP-4 SLA delay flags (#30): 3h named constant, two clocks (unclaimed-since-created, seen-since-seen), needs-attention section + in-row highlight.
- (capability) CAP-5 Secretary add-order form with mandatory Ranch (#32): mirror the mobile admin add-order form (price-catalog pick, delivery now/later, equal/per-stall bag qty, stall multi-select, notes) but add an explicit REQUIRED Ranch dropdown that scopes the stall picker; reuse existing create endpoint.
- (capability) CAP-6 States: loading, empty (no orders), error, and degenerate single-booking-ranch (group-by-ranch collapses to one group gracefully). Reuse summary-page loading/empty/error affordances.
- (constraint) #32 backend path EXISTS (confirmed): POST /api/ShavingsOrders (CreateShavingsOrder) and GET /api/ShavingsOrders/stall-bookings-for-order both authorize via EnsureUserHasAnyRoleInRanch(personId, request.RanchId, RanchAdmin, HostSecretary) - HostSecretary already permitted. usp_createshavingsorder(#169) takes p_ranchid + p_stalls jsonb; usp_getstallbookingsforshavings(#177) feeds the picker. No backend/proc work for #32 reads/writes.
- (decision) Web service work (#32 + list): web/src/services/shavingsOrderService.js currently exports ONLY the approval fns (deleted by Spec 1). Spec 2 adds: getShavingsOrdersForCompetitionAndRanch(competitionId, ranchId), createShavingsOrder(payload), getStallBookingsForShavings(competitionId, ranchId) - mirroring the mobile service. R1/R3 reuse the EXISTING competitionSummaryService.getCompetitionSummaryShavingsDetails/Entries (no new fns).
- (constraint) Design: RTL Hebrew, Tailwind v4, React Router v7. Reuse the competition-summary visual language (SummaryTable, SummaryAmountCards, CompetitionSummarySection palette #3F312B/#8A7268/#E6DCD5, rounded-[28px] cards, #F3EEEA table headers) so the page reads as native. No default/template look; grouping toggle + SLA flags must feel systemic, not bolted on.
- (constraint) Verify in browser preview (web dev server, npm run dev, localhost:5173). Screenshot the ranch view, the status view, and a flagged-delayed order as proof (redesign is observable).
- (question) DEP-1 (Spec 1 dependency, REQUIRED for CAP-3 unverified flag): the handoff names 'unverified delivery' as a first-class Spec-2 concept but #176's exposed field list omits deliveryphotourl, so unverified (delivered AND no photo) cannot be computed. Recommend Spec 1 append DeliveryPhotoUrl (nullable) LAST to usp_getshavingsordersforcompetitionandranch (#176) - return-type change => DROP+CREATE, same discipline as CAP-7. Ownership (fold into Spec 1 vs small Spec-2 backend touch) needs Oren's call.
- (question) DEP-2 (Spec 1 dependency, OPTIONAL/deferred): a competition-scoped order-list read returning ALL booking ranches in one call with BookingRanchId/Name in the output, to replace the client-side per-ranch R2 loop. Not needed for v1 (live has 1 booking ranch); flag for when multi-ranch bookings become real.
- (question) OQ #32 price-catalog source for web: mobile derives the shavings priceCatalogId from getCompetitionInvitationDetails (servicePriceSections). Web add-order form needs the same shavings price(s); confirm the web read (reuse an invitation-details service if one exists, or a dedicated price-catalog-for-shavings read).
- (question) OQ #32 ranch dropdown option set: which ranches populate the required Ranch dropdown? Recommend ranches the secretary is authorized for (HostSecretary in that ranch, matching the create endpoint auth) AND that have pickable stalls. On live data this is just the host ranch. Confirm intended set (host only vs any booking ranch).
- (question) OQ unverified action: Spec 1 left no-photo prominence open. Spec 2 surfaces the unverified flag on the order row; whether the secretary gets a 'request proof' affordance is TBD (no transport - notification track is #31/#46, out of scope). Flag only, no action, in v1.
- (assumption) Spec 1 lands first: CAP-7 fields (Seen/Delivered/PrequestDatetime) on #176 and the token migration are deployed before Spec 2 ships. Spec 2's row rendering and SLA logic depend on those fields being present and populated.
- (assumption) hostRanchId for the reads = activeRole.ranchId, identical to the competition-summary page (CompetitionWorkspaceLayout provides competitionId; ActiveRoleContext provides ranchId). Booking ranch == host ranch on current data.
- (decision) Companions: component-structure.md (page/hook/service/component tree + file plan + web-service additions + states), read-model.md (ranch/status grouping data model, R1/R2/R3 reads, per-ranch loop, client regroup, URL state), sla-rules.md (constant, two-clock rules, needs-attention, SLA state diagram), add-order-form.md (#32 form mirror + required-ranch UX + field mapping from mobile), spec-1-dependencies.md (fields consumed from #176 + DEP-1/DEP-2 + reused procs, the explicit deliverable).
- (note) Success signal: mid-competition, secretary opens the shavings page, sees every order grouped by status, spots the needs-attention orders (one unclaimed >3h, one seen-undelivered >3h) at a glance, flips to ranch grouping, and adds a new order scoped to a ranch's stalls - with no approval step anywhere in the loop.
- (note) Non-goals: data layer/procs/worker-mobile/approval-removal (Spec 1); notification push #31/#46; authoring any new proc; reworking the billing/charge split inside usp_createshavingsorder; the order-detail drill-down richness (R3) beyond a basic expand is optional for v1.
- (event) Self-validate Pass 1 (coherence) PASS: 6 capabilities each carry intent+success; intents state WHAT (HOW lives in companions); all 6 constraints rule out a real design choice (single-writer reuse of Spec 1 reads, #176-as-backbone single-ranch loop, no-approval, DeliveryPhotoUrl dependency, RTL/Tailwind/RRv7 reuse, browser-preview proof); 5 non-goals explicit; success signal is a concrete demonstrable moment (two needs-attention orders, ranch toggle, add-order with no approval); CAP-1..6 stable/unique; prose lean.
- (event) Self-validate Pass 2 (preservation) PASS: every load-bearing source claim landed. #29 -> CAP-1/CAP-2 + read-model + component-structure; summary reuse (#172/#173) -> read-model R1/R3; #30 two rules + named constant + needs-attention/in-row -> CAP-4 + sla-rules; #30 wording -> OQ; #32 mirror + mandatory ranch + backend-exists -> CAP-5 + add-order-form; Spec 1 vocabulary + #176 fields -> constraints + spec-1-dependencies; RTL/Tailwind v4/RRv7/no-template + URL state + axiosInstance chain + no-new-procs + browser-preview proof -> constraints. Explicit Spec 1 fields/reads list delivered as spec-1-dependencies.md (DEP-1 required: DeliveryPhotoUrl on #176; DEP-2 deferred: competition-scoped order read). No wrapper-only drops.
- (event) Spec 2 rendered: SPEC.md + 5 spec-authored companions (component-structure, read-model, sla-rules, add-order-form, spec-1-dependencies) + 2 adopted companions from Spec 1 (spec-2-handoff.md, state-machine.md). 6 capabilities, 5 open questions. Spec finalized pending Oren on the 5 OQs.
