---
topic: RideOn unified push-notification pipeline: per-ranch role-resolved broadcast + targeted Expo push, secretary-triggered; confirm-before-send gate + dedupe/throttle; one pipeline, callers #46/#31/delivery-reminder
updated: 2026-07-24T21:28
---

- (note) Source: _bmad-output/notification-pipeline-scope.md (scope-only, no code), fully absorbed. Covers tracker #31 + #46. ONE pipeline, multiple callers.
- (event) Live re-verify vs Supabase sxplumrexbolpwqacpiz 2026-07-24: personranchrole(personid,ranchid,roleid smallint,rolestatus varchar) Approved=78/Rejected=4/Pending=4; shavingsorder 10 rows workersystemuserid 4 populated; passwordresettoken/notification/notificationtype/personmanagedbysystemuser confirmed.
- (decision) CAP-1 token registration in BOTH mobile apps (RN/Expo): permission -> Expo token -> upsert on login -> prune on rotation/DeviceNotRegistered. The long pole; front-load (P1).
- (decision) CAP-2 recipient-resolution SPs: per-ranch (@ranchid), role-filtered, MUST filter rolestatus='Approved'. #46=roleid 2 admins, #31=roleid 4 field workers.
- (decision) CAP-3 send path controller->BL->DAL->DBServices->stored procs only; manual trigger; transport swappable leg keyed off notificationtype, build Expo only (not hardcoded).
- (decision) CAP-4 sent-notification table = audit+dedupe+throttle seatbelt; throttle check before send WHERE ranchid AND notificationtypeid AND competitionid AND sentdate > now()-interval N min.
- (decision) CAP-5 confirm-before-send: two-step shared component, shows recipient count + resolved ranch + exact text; MANDATORY for broadcasts #46/#31; single-recipient delivery reminder EXEMPT.
- (decision) CAP-6 #46 registration nudge to ranch admins only; payers (roleid 1) never notified; copy WARM never 'lagging'; trigger stays secretary-internal; v1 one competition one push.
- (decision) CAP-7 #31 pending-orders bat-signal ranch-wide to field workers; no per-order targeting at this stage.
- (decision) CAP-8 delivery reminder = third caller via contract seam SendDeliveryReminder(shavingsOrderId); target workersystemuserid -> personid via personmanagedbysystemuser; single-recipient; BLOCKED on Shavings respond action, build seam now.
- (constraint) Push-token table keyed on personid (NOT systemuserid like passwordresettoken). Mirror the precedent's SHAPE (hash+timestamps+upsert/prune), not its key column. systemuser->person bridge confined to the delivery-reminder edge only.
- (constraint) No inline SQL: controller->BL->DAL->DBServices.cs->PostgreSQL stored procedures only; SP dict params bound POSITIONALLY (order must match SP). Auth via UserAccessValidator.EnsureUserHasRoleInRanch.
- (constraint) Live DB writes (two new tables + SPs) shown to Oren as exact SQL and confirmed before running via apply_migration. Do NOT design off RideOnDB/schema.sql (stale); live is ground truth.
- (constraint) Push infra + token registration live in THIS feature branch off main (shared infra, not a shavings branch). Coordinate worker-app startup/notification bootstrap + merge order with the parallel Shavings end-to-end track.
- (direction) Phasing to preserve: P0 storage+resolution -> P1 token registration in BOTH apps (long pole) -> P2 send path+Expo -> P3 callers #46/#31+confirm UX -> P3.5 delivery reminder (blocked) -> P4+ later. #31/#46 are the LAST buttons; plumbing first.
- (direction) Non-goals (parked): multi-competition batched push; SMS/email 2nd channel; automated non-human triggers; A-B #46 copy; deep-link/fg-bg polish; per-order #31 targeting; end-to-end delivery-reminder wiring (blocked).
- (assumption) Swappable copy stored via notificationtype (live has only notificationtypename + notificationtypedescription; no dedicated body column) -> reuse description or add column; flagged as open question.
- (assumption) Delivery-reminder bridge personmanagedbysystemuser carries approvalstatus; assumed only 'Approved' mappings used when translating systemuserid->personid; flagged as open question.
- (question) Exact throttle window N (minutes) per notificationtype; token table column shape (per-device vs per-person, device metadata); precise SendDeliveryReminder signature; approvalstatus filter on bridge; where swappable copy lives in notificationtype; deep-link targets per type (P4).
- (event) Self-validate Pass 1 (Coherence, Spec Law 1-6,8): PASS. 8 capabilities each with intent+success; constraints all rule design out; 7 explicit non-goals; success signal is a concrete secretary->confirm->Approved-recipients-only->Expo demonstration; CAP-1..8 unique/stable. CAP-8 correctly marked contract-only/blocked.
- (event) Self-validate Pass 2 (Preservation): PASS. Every load-bearing scope-doc claim lands: §2 ground truth+§4/§5 storage->data-model.md; §3/§7 callers->callers.md; §9 coordination+§12 phasing->build-phases.md; §13 all 10 locked decisions->kernel constraints/capabilities; §14 open items->Open Questions. Wrapper-only drop (logged, intentional): §7 'prior lineage iss42/party-mode' backstory.
- (event) spec finalized: SPEC.md + 4 companions (data-model.md, callers.md, build-phases.md, architecture-diagrams.md) at _bmad-output/specs/spec-notification-pipeline/. Source notification-pipeline-scope.md fully absorbed.
