---
topic: Competition-board date filter (overlap semantics) + backend log-noise hygiene; retires QA #16≡#68, #34, #35
updated: 2026-07-29T22:47
---

- (event) Create op. Input: spec3 handoff from secretary-qa-cleanup triage. Rich input, express extraction, no elicitation.
- (direction) Oren D1 (locked in triage): competition-board date filter must use OVERLAP semantics (appears if it overlaps the searched window at all), not contained-within.
- (constraint) Proc-only + backend-hygiene scope. Do NOT implement; produce SPEC only. No frontend change (CompetitionsBoardPage.jsx/competitionService.js already pass dateFrom/dateTo unchanged).
- (note) VERIFIED live 2026-07-29: usp_getcompetitionsbyhostranch WHERE uses c.competitionstartdate>=datefrom_param AND c.competitionenddate<=dateto_param = contained-within. Live param names are *_param (ranchid_param,searchtext_param,status_param,fieldid_param,datefrom_param,dateto_param), return string cols are character varying.
- (note) VERIFIED live 2026-07-29: comp 14 'תחרות ריינינג 11+12 2025' (2025-11-04..08, ranch 11) is dropped by contained-within (false) but matched by overlap (true) for a Nov5->30 window. This is the live proof for CAP-1.
- (capability) CAP-1: date-filter overlap. Change the two date WHERE lines to (datefrom_param IS NULL OR c.competitionenddate>=datefrom_param) AND (dateto_param IS NULL OR c.competitionstartdate<=dateto_param). Retires #16 (no-results half) and #68.
- (note) #16 error-banner half already resolved live (proc no longer throws on date params). Only the no-results half survives = #68.
- (decision) REPO DRIFT found (not in handoff): repo file 10_usp_GetCompetitionsByHostRanch.sql is STALE vs live in TWO ways — param names (p_* vs live *_param) AND return types (TEXT vs live character varying). Running the repo file as-is against live would fail 42P13 (cannot change return type). Migration MUST be authored from the live functiondef, changing ONLY the two date WHERE lines; repo file 10 then rewritten to match live verbatim.
- (note) DAL caller safe for CAP-1: CompetitionDAL.GetCompetitionsByHostRanch uses CreateCommandWithStoredProcedure (positional SELECT * FROM fn(@p1..@p6)), 6 params in proc order, reads columns by name via MapCompetition. Overlap change is WHERE-only, return columns unchanged -> zero C# change, no redeploy coupling. Deploys independently, backward compatible.
- (capability) CAP-2 (#34): remove the four Console.WriteLine DB-topology prints in RideOnServer/DAL/DBServices.cs Connect() lines 24-27 (host/port/user/database). Security-flavoured (leaks conn topology+username to Render logs every DB round trip). Priority of Part B. Remove entirely.
- (capability) CAP-3 (#35): remove the six [ISSUE-C] Console.WriteLine debug lines — ClassesInCompetitionController.cs (134-136, 192-194) + ClassInCompetitionDAL.cs (323-325, 328, 334, 351) and their TEMP-DEBUG comments. ALSO remove the now-orphaned private helper CountClassPrizes (ClassInCompetitionDAL.cs 355-364), which exists ONLY to feed those debug lines (grep: 3 refs, all in-file; 2 are the debug callers being removed).
- (decision) Commit discipline: Part A (proc behaviour change, CAP-1) committed SEPARATELY from Part B (log-noise cleanup, CAP-2+CAP-3). Feature branch off main; no merge to main without Oren's approval. Each .cs change followed by dotnet build in RideOnServer/.
- (constraint) Live DB write protocol: capture live functiondef before change (done), show Oren exact CREATE OR REPLACE SQL and get go-ahead, apply via apply_migration, re-read functiondef + re-run the comp-14 ranch-11 overlap smoke test as proof.
- (note) Non-goals: #20 (classes entry-count proc gap, own ticket), #52 (FE change-tracking casing, Spec 2), all frontend (Specs 1&2). Live DB param-type binding for @DateFrom/@DateTo is existing working behavior, untouched.
- (event) Self-validate pass 1 (coherence) + pass 2 (preservation) both PASS; see SPEC verdict.
- (event) Pass 1 coherence PASS: 3 caps each intent+success, intents are WHAT, constraints all bind, 5 non-goals, success signal testable (comp14 returns + empty greps). Pass 2 preservation PASS: every handoff claim landed (D1->CAP-1, proc body/comp14 proof/migration+repo-sync->datefilter companion, #34->CAP-2, #35->CAP-3, commit-split+out-of-scope->constraints/non-goals). Added verified corrections beyond source: repo file 10 drift (param names + TEXT vs varchar), orphaned CountClassPrizes helper, orphaned builder local. Wrapper-only dropped: 'paste into new session' ceremony.
- (decision) CAP-2 open question RESOLVED (Oren asked what the prints do): the four Connect() prints are pure diagnostics — nothing reads them, connection is built from cStr not builder. Decision: remove entirely + drop the now-unused builder local. SPEC open question closed.
- (event) Authored handoff-prompt.md: fully self-contained implementation prompt for a COLLEAGUE (not Oren) to run as a goal session. Embeds live+new proc bodies, all file anchors, verify steps — no local/worktree paths (triage docs live only in Oren's secretary-qa-cleanup worktree, so their findings are embedded, not linked). Instructs new branch fix/qa-16-68-datefilter-hygiene off main, two-commit split, production-write go-ahead, no merge to main.
