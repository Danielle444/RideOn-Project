---
topic: Cohesive error handling + required-field indication across the secretary web app; fix judge form generic error (#55,#5,#2,#4,#17,getErrorMessage dedupe)
updated: 2026-07-29T23:00
---

- (direction) Source: secretary-qa-cleanup triage (branch claude/secretary-qa-cleanup-b6ffc8, triage-and-thread-summary.md) + spec prompt. Retires #55(umbrella),#5,#2,#4, optional #17, + getErrorMessage dedupe. SPEC only, no implementation.
- (decision) D2 (locked, Oren): judge form English first/last name + country OPTIONAL; Hebrew first/last stay MANDATORY. Do not re-litigate.
- (decision) Hebrew user-facing strings need Oren sign-off before finalising; spec lists every proposed/changed string in one companion table.
- (capability) CAP-1 One inline field-error convention: standardize on existing ClassInCompetitionModal pattern (fieldErrors.<key> -> div className 'mt-1.5 text-right text-xs text-red-600'). Verified live in repo lines 548-552.
- (capability) CAP-2 One global/page error convention: ToastMessage (fixed top banner, error palette border-#E7BABA/bg-#FDF4F4/text-#A54848) for page-level async failures; in-modal form-level banner (same palette) for modal submit failures. Define when each is used vs inline.
- (capability) CAP-3 #2 required-field asterisk app-wide via Field.required (Field.jsx line 34 already renders red asterisk). Arena/StallCompound/ServiceProduct modals ALREADY pass required. Enumerate remaining forms/fields by walking each form save/continue validation.
- (capability) CAP-4 #5 judge form field-specific inline Hebrew-name errors replacing generic catch-all (useJudgeCreation.js line 68). English/country optional = CLIENT-ONLY (JudgeModal already omits required on those inputs).
- (capability) CAP-5 #4 registration-end/closing date: add min= on date input + inline invalid-date message (rule registrationEnd>=registrationOpen already exists at save in validateDetailsForm competitionForm.utils.js:165-171). Applies to competition create + edit (shared CompetitionDetailsSection.jsx).
- (capability) CAP-6 (optional #17) prize-type CustomDropdown per row filters options to UNUSED prize types (block duplicate at entry). Duplicate already rejected at save in validatePrizeRows ClassInCompetitionModal.jsx lines 124-128.
- (capability) CAP-7 getErrorMessage dedupe: 3 independent defs (competitionForm.utils.js:88 richest/superset, useCompetitionPaymentsPage.js:34, WorkersManagementPage.jsx:108) -> 1 shared helper (adopt superset). Migrate ~19 importing files.
- (note) SERVER CONFIRMATION (#5, verified live 2026-07-29 project sxplumrexbolpwqacpiz): judge.firstnameenglish/lastnameenglish/country all is_nullable=YES; ZERO check constraints on judge table; BL Judge.ValidateJudge (Judge.cs:71-85) only requires Hebrew first/last + fieldIdsCsv; proc usp_insertjudge does plain INSERT no validation. CONCLUSION: D2 optionality needs NO backend/proc/DB change. #5 is frontend-only.
- (note) Backend ValidateJudge throws English-language messages ('Hebrew first name is required') via generic Exception -> surfaced by getErrorMessage generically. Latent, rarely hit (frontend HTML5 required guards). Client-side field validation (CAP-4) makes it unreachable; not a required backend touch.
- (assumption) Field-using forms already carrying required: ArenaModal, StallCompoundModal, ServiceProductModal (verified). Forms NOT using shared Field (need asterisk work): JudgeModal, ClassInCompetitionModal, PaidTimeSlotInCompetitionModal, CompetitionDetailsSection, AddShavingsOrderModal, superuser modals (ClassTypeModal, FieldModal, FineModal, PrizeTypeModal, ReiningPatternModal, CreateSuperUserModal), WorkersManagement. Per-field mandatory list to be finalized by walking each form validation at implementation.
- (assumption) RegisterScreen.jsx + ChangePasswordPage.jsx use shared Field; asterisk state per-field not individually verified this session.
- (constraint) No web shared Button primitive; button cohesion (103 raw <button> files) is OUT of scope (separate future spec). #57 gender dropdown OUT (sibling pattern, after this). Loading-vs-missing, cosmetic, backend date-filter overlap = Specs 2/3, OUT.
- (constraint) Build/verify gate: web npm run build + npm run lint. NO dotnet build needed (server untouched: #5 confirmed frontend-only).
- (event) Self-validate pass 1 (coherence): all 7 capabilities carry intent+success; constraints bind scope; non-goals explicit; success signal testable. PASS.
- (event) Self-validate pass 2 (preservation): every load-bearing source claim (D2, verified anchors, server confirmation, dedupe list, ticket mapping) routed to SPEC.md or a companion. Wrapper ceremony (triage process notes) intentionally dropped. PASS.
- (event) SPEC finalized: SPEC.md + error-convention.md + change-list.md + hebrew-strings.md. 8 capabilities (CAP-1..8, CAP-7 optional). Both self-validate passes PASS.
- (decision) CAP-2 RESOLVED (Oren): keep uniformity with existing forms. Verified pattern: form-submit rejection surfaces as BOTH in-modal banner (props.error, palette border-#E7BABA/bg-#FDF4F4/text-#A54848) AND page error toast (showToast), both fed by the SAME getErrorMessage string. Class modal (useSecretaryCompetitionClassesPage.js:471-475 + ClassInCompetitionModal.jsx:891-893) and JudgesManagementPage already do this. Judge form must match.
- (decision) CAP-7 (#17) RESOLVED (Oren): IN SCOPE NOW, no longer optional. Implement prize-type dropdown unused-only filtering this batch.
- (decision) CAP-6 (#4) RESOLVED (Oren): guard on BOTH reference dates AND both create+edit. Closing (registrationEnd) date bounded: >= registrationOpenDate (min) AND <= competitionStartDate (max, registration closes before competition begins). Apply min/max + inline message on both create and edit (shared CompetitionDetailsSection.jsx).
- (event) Spec re-derived after 3 resolutions: CAP-2 success updated, CAP-7 promoted to in-scope, CAP-6 dual-bound; Open Questions cleared. Re-validate PASS.
- (event) Handoff authored: implementation-handoff.md — self-contained goal-format prompt, references ONLY global skills (ride-on-system-knowledge, ride-on-live-db-ops), embeds all decisions/anchors/strings/server-confirmation so it needs no external artifact. Added to companions.
