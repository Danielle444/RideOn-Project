# memlog — spec-summary-breakdown-refactor

topic: Frontend-only refactor of the secretary Competition Summary breakdown drill-downs (SummaryDetailsModal) — day-first views, short/long as columns, column removal, shared summary strip.

- event: create — distilled from a completed party-mode triage (Stage 1). All decisions arrived locked; this is a distill, not an elicitation.
- direction: Stage 2 (bmad-spec). Deliverable = SPEC.md + companions + REQUIRED companion implementation-prompt.md (the colleague goal prompt). The colleague prompt is the whole point of the session.
- note: verified against live code — SummaryDetailsModal.jsx (DetailsTable = level-1, EntriesTable = innermost), useCompetitionSummaryPage.js (openDetails → detailsItems flat rows; openEntriesForDetail → entryItems), competitionSummaryService.js (read-only reference).
- note: verified paid-time level-1 row fields: slotDate, startTime, endTime, arenaName, productId, productName, durationMinutes, requestCount, paidTimeSlotInCompId, paidAmount, unpaidAmount, expectedAmount. Entries drill keys on paidTimeSlotInCompId + productId.
- note: verified class level-1 row fields: classDate, orderInDay, className, entryCount, fineCount, classInCompId, paidAmount, unpaidAmount, expectedAmount. Entries drill keys on classInCompId + sectionKey.
- note: verified stall level-1 row fields: bookingRanchName, productName, isForTack, bookingCount, horseCount, tackCount, paidAmount, unpaidAmount, expectedAmount. Entries drill keys on bookingRanchId + productId + isForTack — NOT on horseCount/tackCount, so deleting those two columns cannot break the drill.
- note: verified the modal has NO summary/totals strip today; only page-level cards carry totals.
- decision: CAP-1 — Paid-time becomes day-first. Level 1 = one row per slotDate (short-count col, long-count col, total requests, combined paid/unpaid/total expected). Level 2 = that day's existing per-slot rows. Level 3 = existing EntriesTable (unchanged). No hour/time-of-day tier.
- decision: short vs long distinguished by product identity (productId/productName), NEVER by thresholding durationMinutes. Reason: durationMinutes is a display value, not a category.
- decision: CAP-2 — Classes becomes day-first. Level 1 = one row per classDate (# classes, total entries, fines, paid/unpaid/expected). Level 2 = that day's classes (existing columns). Level 3 = existing EntriesTable (unchanged).
- decision: CAP-3 — Stalls: delete only the סוסים (horseCount) and ציוד (tackCount) columns; keep every other column and the row→entries drill unchanged. Reason: rows are already split by isForTack and by product; horse/equipment counts already live in the overall orders and restate data.
- decision: Shavings — UNCHANGED. Do not restructure. (Non-goal.)
- decision: CAP-4 — One shared summary strip (requests · paid · unpaid · overall total) rendered identically on the top day-list, the drilled-in day, and the stalls/shavings tables. Consistency across categories is a HARD requirement.
- constraint: Frontend-only. No .cs, no stored proc, no DAL/controller, no new endpoint or query param. All grouping/aggregation is client-side reduce() over already-fetched rows.
- constraint: Do not change the innermost EntriesTable column sets.
- constraint: The hook's drill model is 2-level today (detailsItems → entryItems). Classes + paid-time require an intermediate day-group level (3 levels: day-groups → day rows → entries) while stalls + shavings stay 2-level. This is a genuine state-model change, client-side only.
- assumption: Day grouping is a pure client-side reduce — paid-time on slotDate, classes on classDate — both already present on fetched rows; no refetch, no proc.
- assumption: Each level-1 paid-time/class row already carries the id the entries drill needs (paidTimeSlotInCompId+productId / classInCompId), so collapsing rows under a day header preserves the ability to drill to entries from the day's rows.
- question: OQ-1 (explicitly requested by Oren) — should the shared summary strip also sit atop the innermost EntriesTable, or only on the day/slot/category levels? Oren's "in all of them" reads broad. Surface, do not silently decide.
- question: OQ-2 (discovered in investigation) — how does the FE classify a paid-time product as "short" vs "long" with only productId/productName on the row and no backend? Either (a) render one count column per distinct productName dynamically (no hard-coded short/long), or (b) apply a known product→length mapping. Flag for the spec/impl to resolve; do not invent a durationMinutes threshold (banned by the CAP-1 decision).
- event: self-validate pass 1 (coherence) — PASS. Every CAP has intent+success; intents are WHAT; constraints bend design; non-goals explicit; success signal concrete/testable.
- event: self-validate pass 2 (preservation) — PASS. All load-bearing party claims landed in SPEC.md or view-layouts.md. Wrapper-only content (party staging ceremony, keepsake prohibition) dropped by design.
- event: spec finalized — 4 capabilities, 1 spec-authored companion (view-layouts.md), 1 required companion (implementation-prompt.md), 2 open questions.
- decision: OQ-1 RESOLVED by Oren — the shared summary strip renders on EVERY level, including atop the innermost EntriesTable. No longer an open question.
- decision: OQ-2 RESOLVED by Oren — paid-time level-1 renders ONE dynamic count column per distinct productName present in the day's rows (self-describing; no hard-coded short/long, no durationMinutes threshold). No longer an open question.
- note: main has progressed since the party session; colleague branches off CURRENT origin/main.
- event: spec re-derived with both OQs resolved — 0 open questions remaining.
