# .memlog — system-manager-web-fixes

Append-only. Canonical memory; SPEC.md + companions are derived from this.
(bmad `memlog.py` script not present in this worktree — maintained by hand in the
same format.)

- event: init — topic = SuperUser web app fixes, 5 live items + 1 scratched; input = party-mode BRIEF.md (2026-08-02, expanded 2026-08-03).
- note: canonical input BRIEF.md lives in sibling worktree zealous-euclid-264540; read from there. Output written to this worktree's _bmad-output/specs/system-manager-web-fixes/ per the kickoff prompt's explicit path (no `spec-` prefix, unlike sibling folders).
- direction: distill only, do NOT re-triage; treat every BRIEF "Locked decision" as settled; flag genuine contradictions, don't silently change.
- event: verified all BRIEF file:line anchors against live code in this worktree — all held.
- note: authService.js imports the shared "./axiosInstance" (both login + loginSuperUser), confirming CAP-1's interceptor-scoping fix is viable.
- capability: CAP-1 — failed super-user login stays on screen; auth calls opt out of global 401 redirect (axiosInstance.js:25-29); non-auth 401 redirect unchanged.
- capability: CAP-2 — payer pending-registrations 400 fixed server/DB-side + toast hardened; only DB-gated item.
- note: CAP-2 chain verified — superUserService.js:56 → PayersController.cs:55-74 (EnsureSuperUser=403 not 400; catch→BadRequest is the observed 400) → Payer.cs:303 → PayerDAL.cs:466-507 (proc usp_GetPendingPayerRegistrations, empty param dict, reads 10 columns by name).
- assumption: A-1 — CAP-1 scope is the two login POSTs; forgot/reset/OTP left on default unless found to share the symptom.
- question: OQ-1 — CAP-2 exact 400 cause unconfirmed until live proc read + backend log (PayersController.cs:71 prints ex.Message); DAL column-drift is the lead hypothesis, not proven.
- capability: CAP-3 — shared SuperUser form system; build FormModal/FormField/inputs/error on locked canon tokens; migrate all 7 forms.
- decision: CAP-3 = extract shared primitive, NOT align-in-place; screen identity = SuperUser catalog (party's secretary guess was wrong, Oren corrected).
- note: CAP-3 inventory verified present — PrizeTypeModal (exemplar), FineModal, ClassTypeModal (English labels :94/:106), ReiningPatternModal (keep maneuver table), FieldModal (relic → full rebuild), Judges inline in JudgesManagementPage.jsx → extract to modal, CreateSuperUserModal (restyle inputs only, never touch credentials). Field.jsx is a 3rd label style → absorb into FormField.
- capability: CAP-4 — rename משתמשי מערכת → מנהלי מערכת, 4 spots (SuperUsersManagementPage.jsx:98/:101, SuperUsersTable.jsx:39, superUserMenu.js:14); verified.
- capability: CAP-5 — message-standardization contract: one shared string error helper + success toast on every mutation + standard error surface + replace alert(). Verified err.response?.data pattern spans 6 superuser pages; FieldsManagementPage.jsx:149-151 already hand-rolls the extraction (formalize it).
- capability: CAP-6 — Fines edit-only, SCRATCHED, no change. Grounded in BL/Services/FineResolver.cs (auto-fires on active policy + fixed trigger vocab); FinesController GET+PUT only; FinesTable already in product format. Reversed twice; do not reopen without engine change.
- constraint: all new/changed Hebrew user-facing strings need Oren approval (rename copy, ClassType translations, new toast text).
- decision: CAP-2 toast fix uses the CAP-5 helper (not a one-off) — same anti-pattern.
- event: Self-Validate Pass 1 (Coherence, Spec Law 1-6,8) — PASS. All 6 capabilities have intent+success; intents are WHAT not HOW (HOW routed to companions); constraints bend decisions (401 scoping, DB-drift gate, extract-not-patch, credentials off-limits, Hebrew approval); non-goals explicit (secretary, mobile, Fines CRUD, non-auth 401, filter inputs); success signal is demonstrable per item; CAP IDs stable/unique. Weak spots converted to A-1 (assumption) and OQ-1 (open question), no invented content.
- event: Self-Validate Pass 2 (Preservation) — PASS. Every load-bearing BRIEF claim landed: Item1→CAP-1, Item2→CAP-2+backend-changes.md, Item3→CAP-3+decisions D3 (canon tokens + 7-form inventory), Item4→CAP-4, Item5→CAP-5+decisions D5, Item6→CAP-6+decisions D6, plus the reversals/flags (D6 double-reversal, Item3 screen identity, closed-enum disproof) and execution order. Wrapper-only content dropped: BRIEF legend/status/pipeline preamble (process metadata, not contract).
- event: spec finalized — 6 capabilities (5 live + 1 scratched), 3 spec-authored companions (decisions.md, frontend-changes.md, backend-changes.md), 1 assumption (A-1), 1 open question (OQ-1), 0 contradictions.
- event: CAP-2 live investigation (2026-08-03, read-only). ROOT CAUSE CONFIRMED — usp_getpendingpayerregistrations INNER JOINs public.registrationtoken which does NOT exist in any schema; running the proc returns 42P01. Propagates proc→NpgsqlException→PayerDAL.cs:503→PayersController.cs:69-73 catch→400 (exact toast string).
- note: signature/column-drift hypothesis DISPROVEN — proc return signature matches DAL's 10 columns exactly.
- note: systemic — 4 procs depend on the missing table (usp_saveregistrationtoken INSERT, usp_getvalidregistrationtoken, usp_completepayerregistration UPDATE, usp_getpendingpayerregistrations). Whole payer self-registration-token flow is dead on live. systemuser.registrationcompleted column also absent.
- note: repo migration RideOnDB/migrations/add_registration_token_table.sql holds the exact DDL (table + registrationcompleted column, both IF NOT EXISTS) — never applied to live.
- note: prod postgres logs (get_logs, last 24h) = 29 ERROR relation public.registrationtoken does not exist, several within one second (matches repeated-400s screenshot).
- decision: CAP-2 fix = apply the existing repo migration to live (a DB write, additive/backward-compatible). No C# change required. OQ-1 RESOLVED; pending only Oren's go-ahead on the live write.
