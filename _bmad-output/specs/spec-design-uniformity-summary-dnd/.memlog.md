# memlog — spec-design-uniformity-summary-dnd

topic: Homogenize the competition-summary three tabs onto one design language, and make the stalls + paid-time drag-and-drop surfaces render each booking identically across queue/cell/overlay with block-with-notice on occupied targets. Web app only, FE-only, no DB/backend.

---

- event | create — input is the Stage-1 SPEC-SESSION-PROMPT.md (party-mode handoff), pre-sorted into Topic A (summary tabs) + Topic B (dnd consistency). Decisions locked; the "how" is to be specced.
- direction | Ground rules: web-only (RideOnClient/rideon-client/web), zero .cs / zero stored-proc edits, new branch off main, push public, PR only (Oren merges), preserve behavior, match existing code style (var, function decls, camelCase||PascalCase fallbacks, inline Tailwind).
- decision | Actual tab (CompetitionSummarySection) is the canonical house style; Prediction + Comparison adopt its container/header/figure-card language, NOT its sentences.
- capability | CAP-1 (A-shell): extract SummarySectionShell + SummaryFigureCard from the Actual tab; refit CompetitionSummarySection onto them with zero visual change. Foundation for A-track.
- capability | CAP-2 (A-prediction): wrap FinancialProjectionPanel in the shell, BandCard→shared figure card, reconcile the lone #FBEFE7 caption band into the reference palette.
- capability | CAP-3 (A-comparison): wrap FinancialComparisonPanel in the shell, scale header to text-3xl font-black, add context figure cards (predicted band lo–hi, actual, delta), keep green/amber verdict (no red).
- capability | CAP-4 (B-dnd-block): DroppableBox gains a blocked over-state (red ring vs accept-highlight) + reject toast on occupied-target drops, on BOTH stalls and paid-time.
- capability | CAP-5 (B-stalls-name): ranch-first identity on the assigned cell AND the drag overlay (they must be identical); queue stays horse-first (ranch is redundant — sidebar is ranch-scoped by tabs).
- capability | CAP-6 (B-paidtime-name): one shared identity presenter so the resting queue card header and the assigned cell show the same field order (horse → rider•product → coach); overlay already horse-first, keep it.
- capability | CAP-7 (B-paidtime-ergonomics): whole 1fr row is the grab handle (Info/X still fire); bump invisible #F3EAE4 row dividers to a legible token.
- verified | Topic A: CompetitionSummarySection.jsx:137-148 shell + header confirmed; the header row is a grid that ALSO holds the cash/invoice/secondary action buttons — shell must expose an optional actions slot but not own the button logic (guard honored).
- verified | Topic A: SummaryAmountCards AmountCard is a <button> that formats money internally; BandCard is a <div> receiving a pre-formatted range string + an availability/prompt/hint mode. Shared card must be presentation-only (value as a node), support static/clickable + available/prompt + optional hint.
- verified | Topic B stalls: queue getItemTitle=horse-first; StallCell=ranch-first (bookingRanchName bold, horse beneath); overlay getDragTitle=horse-first (the deviation to fix). Queue `item` carries bookingRanchName — confirmed useCompetitionStallsPage.js:195 — so the overlay can lead ranch client-side.
- verified | Topic B toasts: stalls page uses a page-local setToast + ToastMessage and handleDragEnd lives in the page (no hook change for its reject toast). Paid-time handleDragEnd lives in useCompetitionPaidTimePage.js and already has onShowToast wired — reject toast goes there.
- verified | Topic B paid-time ergonomics: divider border-[#F3EAE4] is on the grid row wrapper in CompetitionPaidTimePage.jsx:702, not in the cell. DraggableItem in PaidTimeScheduleCell wraps only the text block; X is a flex sibling (outside the draggable), Info is absolute with stopPropagation. distance:8 activation constraint means a click still fires the buttons.
- assumption | CAP-3 delta figure = signed gap to the nearest band edge (within→₪0, above→+overshoot, below→−undershoot). Chosen because it mirrors the existing verdictLabel semantics and needs no midpoint invention; purely client-side display, no endpoint/money-math change. Flagged as open question for Oren/writer to confirm.
- assumption | Reject-toast Hebrew: stalls "התא כבר תפוס", paid-time "המשבצת כבר תפוסה" — placeholder strings from the prompt; writer/Oren finalizes wording.
- decision | Two independent tracks (A: CAP-1→2→3 ordered, shell first; B: CAP-4→5→6→7) may be parallelized by two colleagues.
- event | Self-validate pass 1 (coherence): PASS — 7 capabilities each carry intent+success; constraints bend design; non-goals explicit; success signal demonstrable.
- event | Self-validate pass 2 (preservation): PASS — every load-bearing claim from the Stage-1 prompt routed to SPEC.md, component-contracts.md, or implementation-prompt.md. No load-bearing drop.
