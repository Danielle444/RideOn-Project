---
topic: Secretary web loading-vs-missing 3-state cohesion + cosmetic (#28,#62) + change-tracking dead-code (#52) cleanup
updated: 2026-07-29T22:35
---

- (direction) Source: Spec 2 handoff (secretary-qa-cleanup triage, 2026-07-29). Cohesion/cleanup pass, NO product decisions. Retires Oren off-list loading-vs-missing bug + #28 + #62 + #52. Express distill from a fully-specified handoff — no elicitation.
- (note) Verified read 2026-07-29: shared primitives exist and work — DataTableLoadingState.jsx (Loader2 spinner row, default 'טוען נתונים...'), DataTableEmptyState.jsx (default 'לא נמצאו נתונים להצגה'), LoadingSpinner.jsx (page-level). Both table states take colSpan + optional message.
- (note) Verified read: reference impl CompetitionsTable.jsx:256-263 is the canonical 3-state — {loading && <Loading/>} then {!loading && rows.length===0 && <Empty/>} then rows. 15 consumers already import the shared empty state (16 grep hits incl definition); 17 import the loading state.
- (note) Verified: three site patterns exist — (P1) correct shared 3-state; (P2) hand-rolled markup BUT loading-gated (WorkersTable.jsx:69-89, ChangeRequestsTable.jsx:257-283) = cosmetic dedup only, no flash bug; (P3) hand-rolled AND no loading gate (ShavingsOrdersTable.jsx:48 ) = the actual flash-missing bug.
- (capability) CAP-1 loading-vs-missing 3-state standard: every secretary/superuser DATA-FETCH list/table renders shared loading while loading===true, shared empty only when loading===false AND data empty, else rows. Migrate hand-rolled empties; thread a loading prop where absent. Success: no surface shows an empty/'missing' message while its fetch is in flight.
- (capability) CAP-2 (#28): remove dead disabled duplicate 'הוסף תא' button CompetitionStallsPage.jsx:198-206 (disabled/cursor-not-allowed/title='יפותח בהמשך'); keep the working one at :224-232 that opens the create modal. Success: exactly one add-stall control, and it works.
- (capability) CAP-3 (#62): SummaryAmountCards.jsx income/paid/unpaid cards overflow horizontally when viewport narrows (text-3xl money in grid <button> cards with no min-w-0/wrap; grid items default min-width:auto). Responsive containment fix. Success: no horizontal page overflow / no clipped figure at 320-1440px.
- (capability) CAP-4 (#52): remove dead camelCase half of change-tracking dual-casing (getValue camel||pascal in ChangeRequestsTable.jsx, ChangeRequestDetailsModal.jsx, useCompetitionChangeTrackingPage.js — API returns PascalCase, verified in system-knowledge) and extract byte-identical buildChangedFields (+splitDetailsText/getDetailLabel/getDetailValue) into existing utils/changeTracking.utils.js. Success: one buildChangedFields, no camelKey lookups, behavior unchanged.
- (decision) Empty-state copy unified to the shared default 'לא נמצאו נתונים להצגה'. Surfaces whose current wording is meaningfully more specific (e.g. 'לא קיימים שופטים להצגה', 'לא נמצאו כניסות להצגה') KEEP their message via the message prop — unification is about the loading/empty MECHANISM, not forcing one string. Any string that actually changes is listed for Oren in hebrew-copy.md.
- (constraint) Do NOT migrate non-data-fetch empties: dropdown/picker search-empty (CustomDropdown, MultiSelectPicker, *noResultsText* props), per-field 'not set' text (ProfileSettingsPage/RanchProfileCard 'לא הוגדר מיקום', StallCell 'ריק'), and auth/error banners (AuthContext, ChangePasswordPage, SelectRanchPage). These are not loading-vs-missing and are out of scope.
- (constraint) Preserve action-bearing empties: ChangeRequestsTable filter-empty (:261-275) carries a 'ניקוי סינון' clear button and is split from the true-empty (:277-283). Do not flatten to a bare DataTableEmptyState. Reuse the loading state, keep the two empties' logic.
- (constraint) Reuse existing primitives only — do not build new components. No server/proc changes. Verify with npm run build + npm run lint (no dotnet). Branch base main. #20 (classes entry-count backend gap) stays OUT.
- (assumption) Tier A (no loading gate → flashes) vs Tier B (gated, cosmetic) per site is grounded in reads for CompetitionsTable/WorkersTable/ShavingsOrdersTable/ChangeRequestsTable and a -reference grep for the rest; the implementer MUST open each enumerated file to confirm its tier before editing (grep count is a prior, not proof).
- (event) Self-validate pending: SPEC.md + companions to be written next.
- (event) Pass 1 (coherence): PASS. 4 capabilities each carry intent+success; constraints bend design (reuse-only, data-fetch-only, preserve action-bearing empties, no-server); non-goals explicit (#20/Spec1/Spec3/buttons/mobile); success signals testable; CAP-1..4 stable.
- (event) Pass 2 (preservation): PASS. All handoff deliverables landed — 3-state standard (SPEC CAP-1 + empty-state-sites.md), full enumerated hand-rolled sites w/ anchors + tiers (empty-state-sites.md), #28/#62/#52 change list w/ line anchors (change-list.md), changed-copy-for-Oren (hebrew-copy.md), build/verify note (SPEC+change-list). Caveat recorded: triage's '31 hand-rolled' reconciled into Tier A/B/M/C split; exact per-file tier is implementer-confirmed on open (grep priors not proof).
- (event) Spec finalized: SPEC.md + 3 companions (empty-state-sites, change-list, hebrew-copy). No product decisions; no open questions blocking. Ready for bmad-quick-dev.
