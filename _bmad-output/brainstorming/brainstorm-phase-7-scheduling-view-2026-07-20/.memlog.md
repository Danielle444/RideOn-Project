---
topic: Phase 7: Scheduling View on the RideOn secretary classes page (Smart Element)
goal: Scope the feature fully before any code is written — surface, columns, math, write path, and copy
mode: partner
updated: 2026-07-21T15:01
---

- (note) Phases 1-6 complete and deployed: model v1 live, entry prediction cache and read/display path merged, verified end to end (commit 83aac17)
- (note) Groundwork seeded but unread by any code: fieldconfig (minutesperentrymin/max, maxhorseclassesperday) and smartconfig (shavingsperstallmin/max, betweenclassgapminutes, latefinishyellow/orange/redhour, flatteningguidance)
- (note) QA tracker at issue 39; open items 37/38/39 parked or blocked and out of scope for this stage
- (direction) Coordination pattern: investigation-first Step 1 report verified against live, decisions relayed as one paste-ready numbered reply, Step 2 diffs before applying, then end-to-end verification on a designated live test competition with DB reads as proof
- (note) Constraints: git hook blocks checkout to main and any command containing merge (merges are Oren's own hands); Claude Code has no DB credentials, all live DB work goes through claude.ai
- (decision) Surface: stay on the secretary classes page where the prediction column already lives; add two far-right columns, predicted schedule and live schedule
- (decision) Predicted is the pre-competition view; live takes over as registrations roll in, with reliance shifting toward live closer to the date
- (decision) Restructure the classes table into expandable groups to fight column overload: one plus for arena and judges, one for financial (prices, prizes), one for schedule
- (decision) No flagging of the manually arranged schedule; only the first class start time matters, later classdatetime values are estimates, and no overhead is pushed onto the secretary
- (decision) Min/max exposed as three separate states (regular / minimum / maximum) rather than a combined band, since a time-span range would confuse
- (decision) Write path: when a day would finish past midnight, suggest an earlier start rounded to 30-minute increments with an accept button; never push earlier than 06:00
- (decision) Scope fence: maxhorseclassesperday, conflicts, and stalls/shavings are all deferred to the next phase
- (decision) All-around exempt classes are excluded from scheduling entirely, with no schedule column shown at all
- (decision) Missing first-class start time: show a generic assumed time plus a please-update nudge, not durations only
- (decision) Earlier-start button reuses the existing class update flow, scoped so only the hour is affected
- (decision) Keep the seeded 23/24/25 late-finish tiers; before midnight show advisory text only, with no actionable button
- (decision) Regular value for a ranged field (extreme, 5-7 minutes) uses the average, 6
- (decision) All schedule math is per day, not per competition: each day rolls forward from its first class starttime and the button writes to that day first class
- (decision) When several first classes share a day (running simultaneously), calculate from the earliest of them
- (decision) Seven Hebrew strings finalized; tooltip keeps the average-duration wording for the regular state despite reining and cutting having a single value (see hebrew-strings.md)
- (question) Parked for a later phase: let the secretary set per-class minutes-per-entry generally, not only for extreme; to be logged in the QA tracker using the standard bug-creation JSON format
- (event) consolidated requirements paste loaded; session opened to scope Phase 7 before any build
- (technique) batch set: Starbursting, Assumption Reversal, Morphological Analysis, Failure Analysis (creative partner mode)
- (insight by coach) BLOCKER: Phase 7 is not unbuilt. Commit 9d6119a 'schedule start' already implements it across 12 files / 914 insertions: classSchedule.utils.js, overview table, page hook, two services, controller endpoints, ScheduleConfig DTO/BL/DAL, and proc 164_usp_GetScheduleConfigByFieldId.sql
- (insight by coach) classSchedule.utils.js already encodes most settled decisions: per-day grouping, tied-first-class origin resolution, betweenClassGapMinutes added between classes, avg/min/max viewMode, exempt via null minutesPerEntry, tier classification, 30-minute ceil rounding, 06:00 floor, suggestion only on orange/red
- (insight by coach) DIVERGENCE from round-2 decision: missing first-class starttime falls back to durations-only (hasOrigin false), which is the option Oren explicitly rejected. No assumed-start constant exists anywhere, and the overview table does not consume dayHasOrigin, so the please-update nudge is unimplemented
- (insight by coach) betweenClassGapMinutes defaults to 0 when unset and is applied only between classes, never before the first
- (insight by coach) 06:00 floor clamps silently; a day needing more than the clamp allows still shows the clamped suggestion with no indication the suggestion is insufficient
- (event by coach) brainstorm paused before technique 1; surfaced prior-art conflict to Oren for a reframe decision
- (decision by user) FIX REQUIRED: missing first-class starttime must use a generic assumed start plus the please-update nudge, not the durations-only fallback currently shipped in classSchedule.utils.js
- (decision by user) betweenClassGapMinutes applies only after a class, and during reining competitions (exact semantics to be confirmed in the audit)
- (decision by user) Confirmed the 5/6/7 minutes-per-entry spacing for extreme against the flattening split; the average of 6 for the regular state stands
- (decision by user) FIX REQUIRED: when the 06:00 floor clamps the suggestion below what the overage actually needs, add explicit text saying the suggested start is still insufficient
- (decision by user) Session reframed from divergent brainstorm to gap audit: what is built in 9d6119a vs what was settled, as a numbered paste-ready Step 1 report
- (direction by user) Oren offering live DB access to Claude Code; every change must be reviewed with him before it is performed
- (insight by coach) BLOCKER verified live: usp_getscheduleconfigbyfieldid does not exist in pg_proc on project sxplumrexbolpwqacpiz. Repo has the .sql file but it was never deployed, so loadScheduleConfig always fails, scheduleConfig stays null, showScheduleColumns is false and the whole feature renders nothing, silently
- (insight by coach) Live smartconfig verified: betweenclassgapminutes=10, latefinish yellow/orange/red = 23/24/25, no duplicate configkeys, configvalue is numeric so the proc needs no cast
- (insight by coach) Live fieldconfig verified: reining 5/5, cutting 2.5/2.5, all-around NULL/NULL (exempt), extreme 5/7. Jumping and dressage have NO fieldconfig row at all, so the proc LEFT JOIN makes them silently exempt too, which was never a decision
- (insight by coach) CORRECTION to my earlier claim: the missing-start-time nudge IS rendered (renderScheduleCell when not hasClockTime and isFirstOfDay). Only the generic assumed start time half is missing, not the nudge
- (insight by coach) Expandable column groups (arena/judges, financial, schedule) from round 1 are entirely unbuilt; the commit appends two columns to a now-16-column table
- (insight by coach) betweenClassGapMinutes is applied uniformly between every pair of classes on every field, contradicting the reining-only intent
- (insight by coach) Finish times past midnight render unwrapped as 24:15 / 25:30 rather than 00:15 / 01:30; never specified either way
- (insight by coach) Write path VERIFIED SAFE: GetClassById fills JudgeIds and Prizes via separate helpers, and ClassPrizeItem round-trips symmetrically as prizeTypeId/prizeAmount, so resending the full row does not wipe judges or prizes
- (question by coach) Open risk to verify on the test competition: classDateTime is timestamptz and is read then resent verbatim by applyStartTimeSuggestion, so a timezone round-trip could shift a class to the adjacent day
- (insight by coach) Schedule columns are rendered first in the DOM, which is correct for a RTL page (first = rightmost); worth one visual confirmation
- (decision by user) Gap semantics resolved: betweenClassGapMinutes never applies before a day first class (current behaviour is correct) and must be removed entirely for every field except reining
- (decision by user) Jumping and dressage being silently exempt is intended and accepted; finding 5 closed, no fieldconfig rows to add
- (decision by user) Flattening model FINAL, supersedes the seeded all-fields description: reining only, and within reining a flattening gap falls both every 5-7 runs AND between each class, with no double flattening when a class boundary coincides with the run interval
- (event) WRITE APPLIED: smartconfig row defaultfirstclassstarthour=8 inserted and verified (updatedat 2026-07-20 17:19:47+00)
- (insight by coach) Statement 2 impact check clean: every layer reads the proc output by column name, never by position, so a 7th return column is inert until DTO/DAL/utils consume it. FieldId also verified to bind as NpgsqlDbType.Smallint via the value-is-short branch, matching p_fieldid smallint
- (event) WRITE APPLIED: proc usp_getscheduleconfigbyfieldid deployed live with 7 return columns; verified by calling it across all six fieldids, exemption returns NULL bounds for all-around/jumping/dressage
- (event) WRITE APPLIED: betweenclassgapminutes description corrected to reining-only with the 5-7 runs plus between-class model, superseding the 2026-07-18 all-fields wording
- (event) WRITE APPLIED: flatteningguidance valuetext corrected to state reining-only scope and the runs-plus-class-boundary model
- (event) Repo file 164_usp_GetScheduleConfigByFieldId.sql synced to the deployed definition (CREATE OR REPLACE, 7th column, deployment note, corrected exemption comment)
- (decision by user) Assumed-start nudge final Hebrew: lots zmanim mevusas al shaat hatchala meshoeret (HH:MM), yesh lehazin shaat hatchala lamiktze harishon al menat lekabel luach zmanim meduyak - see hebrew-strings.md for the verbatim text
- (decision by user) Keep the advance-button suppression on assumed days, and add a separate button that sets the assumed hour as the real start time
- (decision by coach) Recommended and authorised: the set button writes ONE class, not the whole tied first group, because orderInDay can be null across an entire day which would make the tied group the whole day and stamp the assumed hour onto all of it
- (event by COMMITS: 78fcf2f gitignore, 6de7c5a proc 164 sync, 2353fea assumed-start implementation (audit finding 2 closed)) 
- (insight by coach) Scenario D of the logic check confirms audit finding 6 is still live: a day finishing 26:10 yields a clamped 06:00 suggestion that is 30 minutes short of resolving the overage, with nothing telling the secretary so
